TODO - этап 1:
  1.1 - Сделать новый каталог для магазина, 1 поле - ID будет идти в формате int, 2 Title - в формате String,
  1.2 - 3 Price - в формате double. Сделать полную инкапсуляцию и конструктор, в каталоге создаем 2 объекта(каталог1/2)

TODO - этап 2:
  2.1 - в классе Product (добавить) должно быть поле Description - описание;
  Product class должен быть абстрактным
  2.2 - Category (добавить): 1 - ID, 2 - Title, 3 - Description, список ArrayLisr - сохранит в себе список категорий
  2.3 - автогенерация ID, каждый новый шаг - новое ID которое генерируется самостоятельно
  2.4 - наследники Electronic, GardenItem
  2.5 - создаем новый класс: MobileDevice, должен быть наследником электроники
  2.6 - создаем несколько объектов из новых классов в main, добавляем
  2.7 - в Category нужно добавить AddCategory, ShowCategory

TODO - этап 3:
  3.1 - нужно создать класс Category (уже есть), добавить ADD_Category, SHOW_Category
  3.2 - нужно создать счетчик в Category: 1 - сколько категорий, 2 - сколько sub категорий
  3.1 - надо в Category добавить абстрактный метод Show info, который мы будем переобразовывать
  3.2 - создать несколько разных товаров в Product в списке в виде Array list
  3.3 - надо в show info что бы весть список показать на экране

TODO - этап 4:
  4.1 - создать интерфейс payble(помогает объектам товарам, 3 абстрактных метода:
    4.1.1 - double(getFinalPrice),
    4.1.2 - void(pay принимает double, можно назвать amount) - оплата товаров
    4.1.3 - возвращает boolean(itsPayt)
  4.2 создать интерфейс finansable(помогает объектам-клиентам), принимет 3 метода:
    4.2.1 CheckBalance - double
    4.2.2 HasAmountMoney - boolean
    4.2.3 GetFinalStatus - String
  4.3 Необходимо все классы расширять с помощью equals, hashCode, toString, insteadof, DON'T REPEAT
  4.4 создаете объекты в классах и сравниваете

TODO - этап 5:
  5.1 - создать класс Person для покупателя
  5.2 - создать класс Clients для системы работы с покупателем
  5.3 - создать мини банковскую систему для покупки товаров

TODO - этап 6:
  6.1 - добавить сортировку категорий товаров, comparable по возрастанию или по убыванию
  6.2 - надо реализовать компоратор(фильтр по цене/по сезону/по памяти и т.д)
  6.3 - создать пользовательское меню 4 кнопки к примеру:
  (проверить категории(саб категории и категории)/проверить товары/
  критерии сортировки(убывание, возрастание и т.д)/сравнение товаров)
  6.4 - добавить защиту фильтров, чтобы нельзя было сравнить лопату и смартфон и т.д...
  6.5 - создать класс Menu для общего интерфейса с кнопками действий

TODO - этап 7
  7.1 - добавить в проект enum для статуса оплаты, заказов, клиента и.т.д
  7.2 - добавленные enum применяем в коде
  7.3 - добавить как минимум 1 функциональный интерфейс, реализуя возможность в конкретном классе
  7.4 - через lambda реализовать интерфейс
  7.5 - через lambda реализовать интерфейс - через lambda проверка товаров, заказов и.т.д
  7.6 - расширить пользовательский интерфейс, работаем через streamAPI (с коллекциями)

TODO - этап 8
  8.1 - StreamAPI в списках товаров и заказов
  8.2 - расширение пользовательского меню (меню, сортировки)
  8.3 - просмотр списка заказов

TODO - этап 9
  9.1 - фильтрация при помощи streamAPI (firstFind)
  9.2 - SOLID, все принципы solid во всем коде

TODO - этап 10
  10.1 - применять FactoryDesign паттерн для создания товаров, счетов, заказов, клиентов и т.дс
  10.2 - Strategy паттерн переключатель для выбора клиентов или зависимо от входящих данных(например для оплаты)
  10.3 - SingleTon для того чтобы фиксировать определенный класс и не дублировать(каталог)
  10.4 - добавляем DependencyEjection внедряем тот объект что присвоен вне класса(класс не должен быть зависим от других классов)
  10.5 - ImutableObject (использовать printf) история транзакций, покупок, все даты